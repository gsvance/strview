# strview

A string view interface for Python strings

I'm not sure how much sense a string view interface makes in Python as opposed to a lower-lever language, but if you want to reference pieces of a larger string in Python, this will do it.

Somewhat inspired by tsoding's String_View library written in C: https://github.com/tsoding/sv
